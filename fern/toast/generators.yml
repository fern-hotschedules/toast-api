groups:
  publish:
    generators:
      - name: fernapi/fern-typescript-sdk
        version: 0.2.2
        output:
          location: npm
          package-name: '@fern-api/toast'
          token: ${FERN_NPM_TOKEN}
        github:
          repository: fern-hotschedules/toast-node

